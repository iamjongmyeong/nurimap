# Spec: Local Integration QA

## Summary
로그인 전 핵심 사용자 기능을 로컬 환경에서 통합 검증하는 절차를 정의한다.

## Scope
- place 추가 흐름 검증
- 지도/목록/상세 연동 검증
- 반응형 검증
- 주요 실패 상태 수동 검증

## Out Of Scope
- 실제 배포
- 운영 모니터링

## AI Agent Instructions
- 이 spec은 구현 spec이 아니라 검증 spec이다.
- 실패 시 원인을 기록하고 관련 spec 또는 코드 수정으로 되돌아간다.
- 검증 결과는 pass/fail로 남긴다.

## Functional Requirements
- 로컬에서 place 추가 전체 흐름이 동작해야 한다.
- place 추가 UI는 같은 화면 안의 2단계 흐름으로 동작해야 한다.
- 지도, 목록, 상세 상태가 서로 연결되어야 한다.
- 지도 마커 선택으로도 상세 흐름이 열려야 한다.
- 데스크톱/모바일 반응형이 문서 정의와 일치해야 한다.
- 네이버 응답 실패, 좌표 확보 실패, 잘못된 URL 등 주요 오류 상태를 확인해야 한다.
- 잘못된 URL은 inline error, 조회 실패는 modal, 저장 실패는 입력 유지 상태로 검증해야 한다.
- 조회 성공 후 place 이름과 대표 주소가 읽기 전용으로 보이는지 검증해야 한다.
- 신규 생성 성공과 병합 성공이 서로 다른 결과 UX를 가지는지 확인해야 한다.
- 데스크톱 상세는 닫기 액션으로 지도 탐색 상태로 돌아갈 수 있어야 한다.
- 모바일 상세는 전체 화면 페이지로 열리고 지도 복귀 시 선택 상태를 유지해야 한다.
- 같은 사용자는 같은 place에 review 하나만 유지해야 한다.
- 데스크톱 상세는 지도 위에 떠 있는 floating panel로 열리고 지도 배경이 보여야 한다.
- 기존 review가 있는 사용자의 place 등록 시 제목이 `이미 리뷰를 남긴 장소예요`인 modal이 보여야 한다.

## Acceptance Criteria
- place 추가부터 탐색까지 로컬에서 한 흐름으로 검증된다.
- place 추가 UI는 같은 화면 안에서 URL 입력 단계와 등록 입력 단계로 전환된다.
- 데스크톱/모바일 공통 시나리오가 통과한다.
- 주요 실패 상태가 사용자에게 이해 가능한 방식으로 보인다.
- 지도 마커 선택 시 상세 흐름이 열린다.
- 잘못된 Naver Map URL 입력 시 `네이버 지도 URL을 입력해주세요.` 문구가 inline error로 보인다.
- 조회 성공 후 place 이름과 대표 주소가 읽기 전용으로 보인다.
- 조회 실패 시 modal이 표시되고 재시도 또는 닫기가 가능하다.
- 신규 생성 성공과 병합 성공 모두 상세 화면 이동까지 검증된다.
- 데스크톱 상세는 지도 위에 떠 있는 floating panel로 표시된다.
- 데스크톱 상세 닫기 시 지도 탐색 상태로 돌아간다.
- 모바일 상세는 전체 화면 페이지로 표시된다.
- 모바일 상세에서 뒤로 가기 후 지도 화면의 선택한 place가 유지된다.
- 같은 사용자가 같은 place에 두 번째 review를 만들지 않는다.
- 기존 review가 있는 사용자의 place 등록 시 `이미 리뷰를 남긴 장소예요` modal이 표시된다.

## TDD / QA Order
1. 관련 자동화 테스트를 모두 실행한다.
2. 실패 테스트를 정리한다.
3. 데스크톱 수동 QA를 수행한다.
4. 모바일 수동 QA를 수행한다.
5. 실패 상태 QA를 수행한다.
6. 결과를 기록한다.

## Required Test Cases
- place 추가 성공
- place 조회 성공 후 읽기 전용 place 요약 표시
- 중복 place 병합 성공
- place 상세 이동
- 지도 마커 선택 상세 이동
- 데스크톱 floating detail panel 표시
- 데스크톱 상세 닫기 후 지도 복귀
- 지도/목록 선택 상태 연동
- 모바일 목록 페이지에서 상세 이동
- 모바일 상세에서 지도 복귀
- place 등록 초기 review 생성
- 기존 review 존재 시 중복 리뷰 modal 표시
- 중복 리뷰 modal의 `장소 상세 보기` 이동
- 잘못된 URL 실패
- 네이버 응답 실패
- 좌표 확보 실패
- 저장 실패 시 입력값 유지

## Manual QA Checklist
- place 추가 UI가 같은 화면 안에서 URL 입력 단계에서 등록 입력 단계로 전환되는지 확인
- 조회 성공 후 place 이름과 대표 주소가 읽기 전용 요약으로 표시되는지 확인
- 데스크톱에서 place 추가 -> 목록 반영 -> 상세 확인
- 데스크톱에서 지도 마커를 눌러 상세가 열리는지 확인
- 데스크톱 상세가 지도 위에 떠 있는 카드처럼 보이고 뒤에 지도가 보이는지 확인
- 데스크톱 상세를 닫았을 때 지도 탐색 상태로 돌아가는지 확인
- 모바일에서 같은 흐름 확인
- 모바일 목록 페이지에서 상세 진입 확인
- 모바일 상세에서 뒤로 가기 후 지도 복귀 및 선택 상태 유지 확인
- place 등록 후 초기 리뷰와 별점 반영 확인
- 같은 place에 이미 review가 있으면 `이미 리뷰를 남긴 장소예요` modal 표시 확인
- 중복 리뷰 modal의 `장소 상세 보기`로 기존 상세 화면 이동 확인
- 잘못된 Naver Map URL 입력 시 `네이버 지도 URL을 입력해주세요.` inline error 확인
- 조회 실패 시 modal과 `다시 시도` 동작 확인
- 저장 실패 시 입력값 유지 확인
- 중복 등록 시 기존 place 상세 화면 이동 확인

## QA Evidence
- 자동화 테스트 결과
- 데스크톱/모바일 수동 QA 결과
- 실패 상태 검증 결과
