# Spec: Auth Email Login Link

## Summary
회사 이메일 로그인 링크 인증을 정의한다. 이 문서는 인증 구현의 source of truth다.

## Scope
- 허용 도메인 검증
- 로그인 링크 발급
- 로그인 링크 검증
- 인증 실패 피드백
- 요청/검증 비동기 상태
- 최초 로그인 후 이름 수집
- 이름 저장 비동기 상태
- 세션 생성 및 복원
- 로그인 링크 요청 제한

## Functional Requirements
- 허용 도메인은 `@nurimedia.co.kr` 하나다.
- 단, 운영자가 환경변수로 지정한 bypass 이메일은 로그인 링크 검증 없이 바로 로그인할 수 있다.
- 허용 도메인이 아니면 로그인 링크를 발급하지 않는다.
- 허용 도메인이 아닌 이메일 입력은 로그인 화면에서 inline error로 표시하고, 입력한 이메일은 유지한다.
- 로그인 이메일에는 로그인 전용 URL을 포함한다.
- 로그인 전용 URL은 직접 로그인 링크다.
- 로그인 전용 URL 유효시간은 5분이다.
- 새 로그인 링크를 발급하면 이전 링크는 무효화된다.
- 로그인 전용 URL은 한 번 사용하면 다시 사용할 수 없다.
- 동일 이메일 재요청 cooldown은 5분이다.
- cooldown에 걸린 요청은 로그인 화면에서 inline error로 표시하고 남은 대기 시간을 함께 보여준다.
- 동일 이메일 로그인 링크 요청은 하루 최대 5회다.
- 하루 최대 요청 횟수를 초과하면 로그인 화면에서 inline error로 표시한다.
- 로그인 화면에서 처리되는 인증 요청 실패는 입력한 이메일을 유지한다.
- 만료된 링크, 이미 사용한 링크, 새 링크 발급으로 무효화된 링크는 인증 실패 화면으로 처리한다.
- 인증 실패 화면의 기본 CTA는 `새 로그인 링크 받기`다.
- 인증 실패 화면의 보조 CTA는 `이메일 다시 입력`이다.
- `이메일 다시 입력`을 누르면 로그인 화면으로 돌아간다.
- 인증 실패 상태에서는 앱 메인 화면으로 진입할 수 없다.
- 인증 관련 오류는 브라우저 기본 `alert`로 표시하지 않는다.
- 로그인 화면의 요청 상태는 `auth_request = idle | submitting | error`로 관리한다.
- 로그인 링크 진입 검증 상태는 `auth_link_verify = idle | verifying | error`로 관리한다.
- `auth_request = submitting` 동안 로그인 링크 요청 버튼을 다시 누를 수 없다.
- `auth_link_verify = verifying` 동안 인증 결과가 확정되기 전까지 앱 메인 화면으로 진입할 수 없다.
- 로그인 성공 시 같은 브라우저에서 최대 90일 세션을 유지한다.
- bypass 이메일 목록 자체는 환경변수로만 관리하고 tracked source에는 직접 적지 않는다.
- 비로그인 사용자는 보호된 화면과 API에 접근할 수 없다.
- 로그인 성공 후 사용자 이름이 비어 있으면 이름 입력 화면으로 보낸다.
- 이름 입력은 단일 input field 하나로 받는다.
- 이름 input은 비울 수 없다.
- 이름은 1글자 이상 입력되면 유효하다.
- 이름 저장 상태는 `name_submit = idle | submitting | error`로 관리한다.
- `name_submit = submitting` 동안 저장 버튼을 다시 누를 수 없다.
- 이름 저장 실패 시 이름 입력 화면에 머물며 입력값을 유지한다.
- 이름 입력 완료 전에는 앱 메인 화면으로 진입할 수 없다.
- 이름이 이미 있는 사용자는 이름 입력 화면을 건너뛴다.

## Acceptance Criteria
- 허용 도메인이 아닌 이메일은 로그인 링크를 발급할 수 없다.
- 허용 도메인이 아닌 이메일 입력 시 로그인 화면에 inline error가 보이고 입력 이메일이 유지된다.
- 로그인 이메일에는 로그인 전용 URL이 포함된다.
- 로그인 전용 URL만으로 로그인이 가능하다.
- 로그인 전용 URL은 5분 뒤 만료된다.
- 만료된 링크로 진입하면 인증 실패 화면이 보이고 로그인되지 않는다.
- 새 로그인 링크를 발급하면 이전 링크는 인증 실패 화면으로 처리된다.
- 같은 이메일은 5분 내 재요청할 수 없고 로그인 화면에 남은 대기 시간이 표시되며 입력 이메일이 유지된다.
- 같은 이메일은 하루 5회까지만 링크 요청이 가능하고, 초과 시 로그인 화면에서 실패 이유를 확인할 수 있으며 입력 이메일이 유지된다.
- 사용한 링크는 다시 사용할 수 없고 인증 실패 화면으로 처리된다.
- 인증 실패 화면에는 `새 로그인 링크 받기`와 `이메일 다시 입력` CTA가 보인다.
- 인증 실패 화면의 `이메일 다시 입력`을 누르면 로그인 화면으로 돌아간다.
- 로그인 링크 요청 중에는 요청 버튼을 다시 누를 수 없다.
- 로그인 링크 검증 중에는 처리 중 상태가 보이고 결과 확정 전 앱 메인 화면으로 이동하지 않는다.
- 로그인 성공 시 세션이 생성되고 재접속 시 복구된다.
- 환경변수로 지정된 bypass 이메일은 링크 클릭 없이도 로그인된다.
- 로그인 성공 후 이름이 비어 있으면 이름 입력 화면으로 이동한다.
- 이름 input은 한 개이며, 빈 값은 허용되지 않는다.
- 이름은 1글자 이상이면 저장할 수 있다.
- 이름 저장 중에는 진행 중 상태가 보인다.
- 이름 저장 중에는 저장 버튼을 다시 누를 수 없다.
- 이름 저장 실패 시 입력한 이름이 유지된다.
- 이름 입력을 완료해야 앱 메인 화면으로 진입할 수 있다.
- 이름이 이미 저장된 사용자는 이름 입력 없이 앱으로 진입한다.

## TDD Implementation Order
1. 허용 도메인 검증 테스트를 작성한다.
2. 허용 도메인 실패 inline error 테스트를 작성한다.
3. 로그인 링크 발급 테스트를 작성한다.
4. 로그인 링크 만료 테스트를 작성한다.
5. 로그인 링크 재발급 무효화 테스트를 작성한다.
6. 재요청 제한 테스트를 작성한다.
7. 인증 요청 실패 시 이메일 유지 테스트를 작성한다.
8. 단일 사용 테스트를 작성한다.
9. 요청 중 버튼 비활성화 테스트를 작성한다.
10. 링크 검증 중 처리 상태 테스트를 작성한다.
11. 인증 실패 화면 CTA 테스트를 작성한다.
12. `이메일 다시 입력` 로그인 화면 복귀 테스트를 작성한다.
13. 이름 없는 사용자 온보딩 리다이렉트 테스트를 작성한다.
14. 이름 입력 검증 테스트를 작성한다.
15. 이름 저장 중 진행 상태 테스트를 작성한다.
16. 이름 저장 중 버튼 비활성화 테스트를 작성한다.
17. 이름 저장 실패 시 입력 유지 테스트를 작성한다.
18. 이름 저장 후 앱 진입 테스트를 작성한다.
19. 세션 복원 테스트를 작성한다.
20. 보호 라우트 차단 테스트를 작성한다.
21. 구현한다.
22. 전체 테스트를 통과시킨다.

## Required Test Cases
- `@nurimedia.co.kr` 이메일은 허용된다.
- 다른 도메인은 거부된다.
- 다른 도메인 입력 시 inline error와 입력 이메일 유지
- 로그인 링크는 발급 후 5분까지만 유효하다.
- 만료된 링크는 인증 실패 화면으로 이동한다.
- 새 링크 발급 시 이전 링크는 인증 실패 화면으로 처리된다.
- 같은 이메일은 5분 내 재요청할 수 없고 남은 대기 시간이 표시된다.
- 재요청 cooldown 실패 시 입력 이메일 유지
- 같은 이메일은 하루 5회를 초과할 수 없다.
- 하루 제한 초과 시 로그인 화면에서 실패 이유가 표시된다.
- 하루 제한 초과 시 입력 이메일 유지
- 사용한 링크는 재사용할 수 없고 인증 실패 화면으로 처리된다.
- 요청 중 로그인 링크 요청 버튼 비활성화
- 링크 검증 중 처리 상태 표시
- bypass 이메일 즉시 로그인 성공
- 인증 실패 화면의 `새 로그인 링크 받기` CTA
- 인증 실패 화면의 `이메일 다시 입력` CTA
- 인증 실패 화면의 `이메일 다시 입력` 로그인 화면 복귀
- 비로그인 사용자는 보호 화면/API에 접근할 수 없다.
- 이름이 없는 사용자는 로그인 후 이름 입력 화면으로 이동한다.
- 이름이 있는 사용자는 로그인 후 바로 앱으로 진입한다.
- 이름 input이 빈 값이면 저장할 수 없다.
- 이름 input에 1글자 이상 입력하면 저장할 수 있다.
- 이름 저장 중 진행 상태 표시
- 이름 저장 중 버튼 비활성화
- 이름 저장 실패 시 입력 유지

## Manual QA Checklist
- 허용 도메인 이메일로 로그인 링크 요청이 된다.
- 허용 도메인이 아닌 이메일로 요청이 막힌다.
- 허용 도메인이 아닌 이메일 요청 시 입력값이 유지되고 inline error가 보인다.
- 이메일에서 로그인 링크를 클릭하면 로그인된다.
- 만료된 링크는 실패한다.
- 재발급 후 이전 링크는 실패한다.
- 로그인 링크 요청 중 버튼이 비활성화된다.
- 로그인 링크 검증 중 처리 상태가 보인다.
- 인증 실패 화면에서 `새 로그인 링크 받기`와 `이메일 다시 입력`이 동작한다.
- 재요청 cooldown 시 남은 대기 시간이 보인다.
- 재요청 cooldown 또는 일일 제한 초과 시 입력한 이메일이 유지된다.
- 인증 실패 화면에서 `이메일 다시 입력`을 누르면 로그인 화면으로 돌아간다.
- bypass 이메일은 버튼 문구 변경과 무관하게 같은 인증 액션에서 동작한다.
- 이름이 없는 계정은 로그인 후 이름 입력 화면으로 이동한다.
- 이름 input 하나만 보이고, 빈 값으로는 제출할 수 없다.
- 이름 저장 중 진행 상태가 보인다.
- 이름 저장 중 버튼이 비활성화된다.
- 이름 저장 실패 시 입력한 이름이 유지된다.
- 1글자 이상 입력하면 저장되고 앱으로 진입한다.
- 로그아웃 후 보호 화면 접근이 막힌다.

## QA Evidence
- 테스트 실행 결과
- 이메일 링크 수동 검증 결과
- 이름 입력 온보딩 수동 검증 결과
- 세션 복원/로그아웃 검증 결과
