# Context Snapshot — AGENTS.md governance refactor

- task statement: Create a consensus plan for optimizing AGENTS.md so operating_principles stays minimal and task-specific guidance (planning, design, development, QA) moves into referenced governance files.
- desired outcome: A plan document that defines the target AGENTS.md shape, governance file taxonomy, migration sequence, validation criteria, and risk controls; no oh-my-codex core behavior changes required.
- known facts/evidence:
  - `AGENTS.md` is 371 lines / 20,803 bytes / ~2,555 words.
  - `operating_principles` alone is 21 lines / ~342 words and mixes true principles with repo-specific operating guidance.
  - Existing governance docs already live in `docs/00-governance/` (`ai-agent-workflow.md`, `definition-of-ready.md`, `definition-of-done.md`, `docs-structure.md`).
  - Current project docs tree includes product, architecture, specs, design, sprints, history under `docs/`.
  - OpenAI Codex docs say AGENTS.md works well as a simple markdown/project-doc mechanism, supports nested/localized instruction files, alternative filenames, and a configurable max-byte budget.
- constraints:
  - Do not change oh-my-codex specific sections unless necessary.
  - Need a plan, not implementation.
  - User wants governance-folder references from AGENTS.md for context engineering.
  - Must use ralplan consensus style (planner -> architect -> critic).
- unknowns/open questions:
  - Whether to use a new root `governance/` folder or extend `docs/00-governance/`.
  - How much repo-specific instruction should stay inline in AGENTS.md versus referenced.
  - Whether task-specific rules should be split by discipline, trigger, or lifecycle.
- likely codebase touchpoints:
  - `AGENTS.md`
  - `docs/00-governance/*.md`
  - potential new `governance/*.md` files if adopted
  - `.omx/plans/` for final planning artifacts
